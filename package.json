{
  "name": "action-against-islamophobia",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p build && sass sass/main.scss build/css/style.css && cp src/js/views/*.html index.html build/ && cp -r assets build/",
    "preview": "vite preview"
  },
  "author": "Dave Krisopras Essanto",
  "license": "ISC",
  "description": "A web application to combat Islamophobia in Australia",
  "devDependencies": {
    "@vitejs/plugin-legacy": "^6.0.2",
    "sass": "^1.86.0",
    "serve": "^14.2.4",
    "vite": "^6.2.2"
  }
}
